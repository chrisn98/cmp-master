package Chp3_5;

public class MathClass {
	/* Notes:
	 * All the methods in the mass class are static methods also called class methods
	 * they can be invoked through the name of the class in which they are defined
	 * 		without having to instantiate an object of the class first
	 * You can pass an integer to a method that accepts a double parameter
	 * This is a form of assignment conversion
	 * 
	 * The discriminant must be negative if not NAN occurs
	 */
}
